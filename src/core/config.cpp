#include "langchain/core/config.hpp"
#include <algorithm>
#include <thread>

namespace langchain {

// Implementation is mostly in the header since these are simple data structures
// This file can contain more complex implementations if needed in the future

} // namespace langchain